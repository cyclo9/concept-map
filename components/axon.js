import React, { useState, useEffect, useRef } from 'react';

export default function Axon(props) {
    const [id, setId] = useState(props.id);
    const [from, setFrom] = useState(props.from);

    return (
        <>
            
        </>
    )
}